# GlitchGrid Environment Variables
# Copy this file to .env.local and fill in any required values

# Currently, GlitchGrid runs entirely client-side and has no required environment variables.
# All settings are stored in localStorage (theme preference, sound mute state).

# Future optional configurations:
# VITE_API_URL=http://localhost:3000
# VITE_ENABLE_ANALYTICS=false
